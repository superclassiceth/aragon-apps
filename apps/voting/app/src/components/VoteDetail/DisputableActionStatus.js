import React from 'react'
import { Box, GU, textStyle, useTheme } from '@aragon/ui'

function DisputableActionStatus({}) {
  const theme = useTheme()

  return (
    <React.Fragment>
      <Box heading="Disputable Action Status">DisputableActionStatus</Box>
    </React.Fragment>
  )
}

export default DisputableActionStatus
